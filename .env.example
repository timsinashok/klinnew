ANTHROPIC_API_KEY=

# Neon Postgres — paste the pooled or direct DSN from the Neon console.
# Accepts both postgres:// and postgresql:// forms; the API normalizes
# to postgresql+psycopg:// internally. Leave blank to run without DB
# (frontend falls back to localStorage-only mode).
DATABASE_URL=
